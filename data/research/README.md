# Research in progress

One file per city, appended by a research pass as each candidate is verified, so a pass that dies halfway leaves work behind instead of nothing. A finished city's file can be deleted once its data/cities entry ships.
