// The words on every search field in the app.
//
// One constant because there are four fields and they had four sentences
// between them, one of which rotated a noun under your eyes (Hidde,
// 2026-08-22: "verwijder maar die hele functionaliteit en zet er ook gewoon
// in, net zoals op de andere pagina"). The website says the same thing.

import Foundation

enum Search {
    static let placeholder = "Search a place, a tree or a species"
}
