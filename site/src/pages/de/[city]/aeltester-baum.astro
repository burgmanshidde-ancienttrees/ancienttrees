---
// German question page, at the phrase a German speaker actually searches.
// Routing only; TranslatedQuestionPage.astro is shared (Contract J v1.13).
import { getCollection } from "astro:content";
import TranslatedQuestionPage from "../../../components/TranslatedQuestionPage.astro";
import { cityIsRenderable } from "../../../lib/trees";
import { translatedQuestionPaths } from "../../../lib/i18n";

export async function getStaticPaths() {
  return translatedQuestionPaths("de", (await getCollection("cities")).filter(cityIsRenderable) as any);
}
const { city, tr } = Astro.props as any;
---
<TranslatedQuestionPage lang="de" city={city} tr={tr} />
