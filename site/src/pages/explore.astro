---
// The English map. Three lines of routing; the whole implementation is
// ExploreMap.astro, shared with every translated /[lang]/explore so the two
// can never drift (CLAUDE.md, 2026-08-25: a decision travels once, never as
// the same rule written twice).
import ExploreMap from "../components/ExploreMap.astro";
---
<ExploreMap lang="en" />
